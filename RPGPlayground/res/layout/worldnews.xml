<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/lWorldNews"
    android:layout_width="match_parent"
    android:layout_height="35dp"
    android:background="#000"
    android:visibility="invisible" >

    <TextView
        android:id="@+id/tvWorldNews"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@android:color/white"
        android:textSize="24dp"
        android:ellipsize="marquee"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:text=" -- This is an alert of the World News Ticker Service -- If you are reading this alert, it means that the World News Ticker is working for you. -- Wonderful!" />

</LinearLayout>